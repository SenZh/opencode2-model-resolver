# 测试审查 — limit 解析接入 models.dev

## 一、主 agent 自审

| 检视项 | 结果 | 说明 |
|-------|------|------|
| 契约条目是否全覆盖 | ✅ | 主路径-1~5、边界-1~6、相邻-1~3 逐条有对应用例 |
| 用例是否可证伪 | ✅ | 关键断言用引用相等 / 具体数值，非 `!==undefined` |
| 是否覆盖端到端 | ✅ | index-scan.test.ts 驱动 resolveProviderModels |
| 测试是否隔离 | ✅ | 临时目录 + 环境变量隔离，运行前后真实缓存无变化 |
| 通过率 | ✅ | 55/55 |

## 二、sub-agent 独立审查（第一轮）

第一轮提出 1 个 P0 + 4 个 P1 + 5 个 P2。核心发现：

| # | 类型 | 问题描述 | 引用位置 | 严重度 | 是否解决 | 解决方式 |
|---|------|---------|---------|--------|---------|---------|
| 1 | 覆盖完整性 | 主路径-4 端到端扫描路径零覆盖 | 契约主路径-4 | 高 | ✅ | 抽取 `resolveProviderModels` 纯函数 + 新增 index-scan.test.ts（7 例） |
| 2 | 测试有效性 | `buildCachedEntry` output=0 防护漏测（实现也只校验 context） | index.ts:41-46 | 高 | ✅ | 实现补 output>0 校验；补 3 个用例 |
| 3 | 测试隔离 | 测试污染用户真实 ~/.cache 目录 | cache-store.test.ts:13 | 高 | ✅ | ModelCacheStore 支持注入 + 惰性求值；测试用 mkdtemp + 环境变量 |
| 4 | 文档准确性 | 用例数失实（7 vs 8、12 vs 13） | test-result.md:16-18 | 中 | ✅ | 按实测重写 |
| 5 | 测试隔离 | `memoryCache` 单例跨用例串扰 | models-dev.ts:22 | 中 | ✅ | 新增 `__resetModelsDevCacheForTest` |
| 6 | 测试有效性 | v2-catalog 的 models.dev 层实际未生效 | v2-catalog.test.ts:59-66 | 中 | ✅ | mock 按 URL 区分，断言 655360 以区分来源 |
| 7 | 边界覆盖 | parseCachedModels 未测分支 | index.ts:52-67 | 中 | ✅ | 补字符串版本号、models:null、顶层类型用例 |

## 三、sub-agent 独立审查（第二轮复核）

第二轮逐条复核修复情况，做了 3 组变异实验：

| 变异操作 | 预期 | 实际 | 结论 |
|---------|------|------|------|
| 移除 `prev.limit.output > 0` | 用例失败 | 2 例失败 | 断言非空洞 |
| models.dev devInfo 置 undefined | v2-catalog 失败 | Expected 655360 / Received 131072 | 断言可证伪 |
| 移除 afterAll 隔离清理 | 写入临时 home | 确认写入 tmp | 隔离真实生效 |

**复核发现的遗留问题**：文档用例数未随代码更新（宣称 42，实际 55；index-scan 整表缺失；test-plan 的 TC-04 操作名滞后；injectV1Config 零测试覆盖）。

> 注：下表的「引用位置」为**修复前**的行号，现文档已按修正后内容重写，故旧文本不可回溯，特此说明。

| # | 类型 | 问题描述 | 引用位置（修复前） | 严重度 | 是否解决 | 解决方式 |
|---|------|---------|---------|--------|---------|---------|
| 8 | 文档失实 | test-result 总数 42（实 55）、分文件明细错误、index-scan 缺失 | test-result.md:7,14-22（修复前） | 高 | ✅ | 重写汇总与分文件表 |
| 9 | 文档失实 | test-cases 追加用例表遗漏 index-scan 7 例 | test-cases.md:26-34（修复前） | 中 | ✅ | 补端到端用例章节 |
| 10 | 文档失实 | test-result 回归基数「19 例」口径不清 | test-result.md（修复前） | 中 | ✅ | 统一为「回归 20 例 / 全量 55 例」 |
| 11 | 文档滞后 | test-plan TC-04 操作名写 buildCachedEntry（应 resolveProviderModels） | test-plan.md:33 | 低 | ✅ | 全表操作列显式化，消除「同上」歧义 |
| 12 | 覆盖缺口 | injectV1Config 零测试覆盖而契约宣称覆盖 | contract.md:19 | 低 | ✅ | 契约改为显式声明，记为遗留 |

## 四、裁决结论

**差异点仲裁**：两轮审查提出的问题全部采纳。第一轮 P0（端到端缺失）经查属实——我修的是 index.ts 扫描路径却无测试触达，这是最严重的方法论失误。第二轮复核确认代码与测试实现层面无缺陷（变异实验证明），仅文档未同步。

**审查质量校验**：
- 每个发现是否有具体引用？✅
- 维度覆盖是否完整？✅ 6 维度
- 严重度是否有区分？✅
- 是否独立分析而非复述？✅ 第二轮做了 3 组变异实验

**结论**：两轮审查均有效，全部发现已解决 → **测试审查通过**。

## 五、遗留问题（转入 Phase 3）

1. `config-injector-v1.ts`（V1）无生产调用点且无测试覆盖 → 需确认是否死代码。另：该文件首行注释声称「绝不回写污染磁盘文件」，但第 160 行存在 `modelCacheStore.save(...)` 磁盘写入，注释与行为矛盾。
2. `resolveModelLimit` 分支边界未穷尽（服务端字段别名、customDefaultLimit 合并、无效正则退化）。
3. `lookupModelsDev:99` 硬编码白名单、最长前缀优先 → 脆弱性优化。
4. 生图模型（`gpt-image-*`）是否应从过滤层排除 → 产品决策。
